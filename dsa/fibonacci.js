// test Node CLI
function thisLoop() {
    let arr = ['nate', 'waffles', 'paulina']

    for (element in arr) {
        // Output each element of array
        console.log(arr[element])
    }
}

function fibsLength(length) {
    myArr = []
    if (length == 0) {
        m
    }
}

function fibonacci(array) {

}